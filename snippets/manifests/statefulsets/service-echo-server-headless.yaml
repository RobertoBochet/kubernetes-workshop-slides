apiVersion: v1
kind: Service
metadata:
  name: echo-server-headless
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: echo-server
  ports:
    - name: http-service
      targetPort: http
      port: 8080
